# ── Core RAG pipeline ─────────────────────────────────────────────────────────
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0
groq>=0.9.0
python-dotenv>=1.0.0

# ── Vector DB — Pinecone (cloud) ──────────────────────────────────────────────
# Uses native Pinecone SDK directly — no langchain-pinecone needed
pinecone>=5.0.0

# ── Data & charting ───────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.7.0
openpyxl>=3.1.0          # XLSX parsing for TRADESTAT export files

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Live RSS pipeline ─────────────────────────────────────────────────────────
feedparser>=6.0.0
apscheduler>=3.10.0
beautifulsoup4>=4.12.0

# ── Document parsing ──────────────────────────────────────────────────────────
markitdown[pdf]>=0.1.0    # Microsoft MarkItDown — PDF/XLSX/DOCX → Markdown (better table extraction)

# ── Hybrid search ─────────────────────────────────────────────────────────────
rank_bm25>=0.2.2          # BM25 sparse retrieval (pairs with dense for RRF fusion)

# ── Gravity model ─────────────────────────────────────────────────────────────
statsmodels>=0.14.0       # OLS regression for gravity model coefficients
xgboost>=2.0.0            # XGBoost for non-linear trade flow predictions

# ── Observability (optional) ──────────────────────────────────────────────────
langfuse>=3.0.0

# ── Validation ────────────────────────────────────────────────────────────────
pydantic>=2.0.0

# ── Legacy UN Comtrade scripts ────────────────────────────────────────────────
comtradeapicall>=1.3.0
requests>=2.31.0
